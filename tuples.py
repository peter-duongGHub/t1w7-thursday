# Creating a tuple
my_tuple = (1, 2, 3, 'a', 'happy')

# Accessing elements
my_tuple[3]

# Attempting to change an element (Will raise an error)
# my_tuple[0] = 10 

# Tuples unpacked
my_tuple = (a, b,c,d,e)

# Tuples can contain other tuples (nested tuples)
nested_tuple = (1, (2,3), (4,5), (0,0))
# nested_tuple[1][1] get 2nd element of element 2nd

